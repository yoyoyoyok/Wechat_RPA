# Web GUI 与管理功能改造计划

## 一、改造目标

将当前单页采集控制台改造成参考图风格的桌面 Web 管理后台：

- 顶部产品栏
- 左侧功能导航
- 公众号分组侧栏
- 主区域数据表格
- 顶部筛选、排序、批量操作工具栏
- 保留现有采集能力，并强化公众号、任务、文章的管理体验

只改造当前项目已有业务，不增加推送同步、用户体系、云端同步等当前项目不具备的功能。

## 二、页面结构

### 1. 公众号管理

主页面仿照参考图，作为默认首页。

左侧分组：

- 全部公众号
- 未分组
- 自定义分组
- 新建分组

主表格字段：

- 勾选框
- 序号
- 公众号名称
- 启用状态
- 默认采集篇数
- 已采集文章数
- 最近采集时间
- 最近任务状态
- 操作

顶部工具栏：

- 添加公众号
- 批量删除
- 批量启用/停用
- 批量创建采集任务
- 关键词搜索
- 分组筛选
- 状态筛选
- 排序

公众号详情支持：

- 修改名称
- 修改默认采集数量
- 启用/停用
- 设置所属分组
- 查看该公众号文章
- 查看该公众号任务记录
- 删除公众号配置

### 2. 任务中心

集中展示当前任务和任务队列：

- 当前运行任务
- 任务阶段
- 收藏进度
- 链接提取进度
- 暂停、继续、停止
- 队列任务列表
- 失败任务重试
- 已停止任务继续
- 批量删除已结束任务
- 显示“复制后移除收藏”状态

保留现有全局快捷键：

- `Ctrl + Alt + P`
- `Ctrl + Alt + S`

### 3. 文章库

文章列表改造成可管理数据表格：

- 勾选框
- 标题
- 文章链接
- 所属公众号
- 采集时间
- 来源任务
- 操作

支持：

- 标题、链接搜索
- 公众号筛选
- 时间筛选
- 批量删除
- 打开文章
- 复制链接
- 导出 CSV
- 按公众号导出
- 全量导出

### 4. 运行概览

展示当前项目已有数据，不引入额外采集能力：

- 公众号数量
- 已采集文章数量
- 今日采集数量
- 成功任务数量
- 失败任务数量
- 当前队列状态
- 最近任务列表
- 最近采集文章

### 5. 设置

只包含当前项目相关设置：

- 服务地址和端口提示
- 默认采集篇数
- 是否默认启用“复制后移除收藏”
- 日志显示数量
- 页面刷新间隔
- 数据库路径提示
- 数据导出入口
- 快捷启动说明

不增加登录、权限、云同步等功能。

## 三、数据与接口改造

### 数据模型

在现有 `accounts` 表基础上增加公众号管理字段：

- `group_id`
- `enabled`
- `default_number`
- `created_at`
- `updated_at`

新增公众号分组表：

- `id`
- `name`
- `sort_order`
- `created_at`
- `updated_at`

任务表保留历史兼容，同时补充：

- `account_id`
- `remove_favorite`
- `retry_count`
- `started_at`

文章表补充：

- `task_id`
- `account_id`

历史任务和文章中的公众号名称继续保留，避免旧数据失效。

### 公众号接口

- `GET /api/accounts`
- `POST /api/accounts`
- `PATCH /api/accounts/{id}`
- `DELETE /api/accounts/{id}`
- `POST /api/accounts/batch-enable`
- `POST /api/accounts/batch-disable`

### 分组接口

- `GET /api/account-groups`
- `POST /api/account-groups`
- `PATCH /api/account-groups/{id}`
- `DELETE /api/account-groups/{id}`
- `POST /api/account-groups/{id}/accounts`

### 任务接口

保留现有接口，并增加：

- `GET /api/tasks` 支持状态、公众号、时间筛选
- `POST /api/tasks/batch`
- `POST /api/tasks/{id}/retry`
- `POST /api/tasks/batch-delete`
- `POST /api/tasks/clear-finished`

### 文章接口

保留现有接口，并增加：

- `GET /api/articles` 支持分页、搜索、公众号和时间筛选
- `DELETE /api/articles`
- `GET /api/articles/export`
- `POST /api/articles/copy`
- `GET /api/articles/stats`

## 四、视觉与交互规范

- 使用参考图的蓝色主色和浅灰背景。
- 顶部固定导航栏。
- 左侧固定导航栏。
- 公众号管理使用高密度数据表格。
- 表格表头固定，内容区独立滚动。
- 批量操作必须先勾选数据。
- 删除操作必须二次确认。
- 任务运行状态使用颜色标签。
- 空列表、加载中、失败、服务未启动均提供明确状态。
- 所有按钮显示明确结果提示。
- 长标题、长链接使用截断和悬浮完整内容。
- 桌面 Web 优先，重点适配 1920×1080 和 1280×720。
- 保留窄窗口下的水平滚动，不强行压缩表格字段。

## 五、实施顺序

1. 建立新版后台整体布局和导航框架。
2. 重做公众号管理页面。
3. 增加公众号分组和批量任务管理。
4. 重做任务中心。
5. 重做文章库和数据导出。
6. 增加运行概览和设置页面。
7. 补充数据库迁移。
8. 保持现有 API 兼容。
9. 完成浏览器视觉检查和接口回归。
10. 使用真实微信执行多公众号连续采集测试。

## 六、验收标准

- 页面整体结构与参考图一致：顶部栏、左侧导航、分组侧栏、主表格。
- 可以维护公众号、分组和默认采集数量。
- 可以勾选多个公众号连续创建任务。
- 任务支持暂停、继续、停止、失败重试。
- 支持复制链接后自动移除微信收藏。
- 文章支持搜索、筛选、批量删除和 CSV 导出。
- 原有数据可以正常读取。
- 旧接口继续可用。
- 1920×1080 和 1280×720 浏览器窗口无明显重叠或布局错乱。
- 真实微信连续执行多个公众号任务成功。
- 服务重启后数据和公众号配置仍然保留。

## 七、明确不纳入本轮

- 微信消息推送同步
- 云端数据同步
- 用户登录和权限系统
- 多用户协作
- 公众号头像自动抓取
- 公众号推送总数统计
- 移动端 App
- 微信公众号后台 API 接入
